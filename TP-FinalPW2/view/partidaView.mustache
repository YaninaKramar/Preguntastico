<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Partida</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(to bottom, #a8e6ce, #ffffff);
            min-height: 100vh;
            margin: 0;
            font-family: Arial, sans-serif;
            text-align: center;
            padding: 20px;
        }

        .pregunta-container {
            background-color: #9f4c2a;
            border-radius: 20px;
            padding: 20px;
            max-width: 700px;
            margin: auto;
            box-shadow: 0 4px 10px rgba(0,0,0,0.2);
            color: white;
        }

        .categoria {
            background-color: #ff6f20;
            border-radius: 20px 20px 0 0;
            padding: 10px;
            font-weight: bold;
            font-size: 30px;
            color: black;
        }

        .pregunta {
            background-color: white;
            color: black;
            margin: 20px 0;
            padding: 20px;
            border-radius: 15px;
            font-size: 18px;
        }

        .opcion {
            background-color: white;
            color: black;
            margin: 10px;
            padding: 10px;
            border-radius: 20px;
            cursor: pointer;
            transition: all 0.2s ease-in-out;
            border: none;
            width: 100%;
            max-width: 300px;
        }

        .opcion:hover {
            background-color: #dcdcdc;
        }

        .boton-volver {
            position: absolute;
            left: 20px;
            top: 20px;
            background-color: #003b5c;
            color: white;
            border-radius: 20px;
            padding: 10px 20px;
            font-weight: bold;
            text-decoration: none;
            display: inline-block;
        }

        .tiempo {
            font-size: 30px;
            font-weight: bold;
            margin-bottom: 20px;
        }

        .reportar-btn {
            background-color: red;
            color: white;
            border: none;
            font-size: 12px;
            padding: 5px 10px;
            border-radius: 10px;
            float: right;
            margin-top: -40px;
            margin-right: 10px;
        }

    </style>
</head>
<body>

<a href="lobbyView.mustache" class="boton-volver">Volver al lobby</a>

<div class="tiempo">Tiempo restante: {{tiempo}}</div>

<div class="pregunta-container">
    <div class="categoria" style="color: {{categoria_color}};">{{categoria_nombre}}</div>

    <button class="reportar-btn">Reportar Pregunta</button>

    <div class="pregunta">{{pregunta}}</div>

    <div class="d-flex flex-wrap justify-content-center">
        {{#opciones}}
            <button class="opcion" type="submit" name="respuesta_usuario" value="{{numero}}">
                {{texto}}
            </button>
        {{/opciones}}
    </div>
</div>

</body>
</html>
