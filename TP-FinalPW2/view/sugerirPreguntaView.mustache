<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Crear Nueva Pregunta</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body{
            min-height:100vh;
            margin:0;
            font-family:Arial, sans-serif;
            background:linear-gradient(to bottom,#b2e5d0 0%,#ffffff 70%);
        }
        .btn-rounded{
            border-radius:20px;padding:8px 20px;
        }
        .btn-darkblue{
            background-color:#00334e;color:#fff;text-decoration:none;
        }
        .btn-darkblue:hover{
            background-color:#005b8e;color:#fff;
        }
        .btn-orange{
            background-color:#ff6600;color:#fff;
        }
        .btn-orange:hover{
            background-color:#ff7f26;color:#fff;
        }
        .question-card{
            border:1px solid #000;
            border-radius:20px;
            background-color:#ffffff;
            padding:30px 40px;
            max-width:600px;
            width:100%;
        }
        .lobby-btn{
            position:fixed;
            left:20px;
            top:50%;
            transform:translateY(-50%);
        }
    </style>
</head>
<body class="p-4">
<div class="d-flex justify-content-between align-items-start mb-4">
    <a href="" class="btn-darkblue btn-rounded">Mi Perfil</a>
    <h1 class="mx-auto fw-bold text-center">Sugerir Pregunta</h1>
    <a href="" class="btn-darkblue btn-rounded">Cerrar Sesión</a>
</div>

<a href="" class="btn-darkblue btn-rounded lobby-btn">Volver al lobby</a>

<div class="d-flex justify-content-center">
    <form action="" method="post" class="question-card">
        <div class="row g-3">
            <div class="col-md-6">
                <label class="form-label fw-bold">Categoría</label>
                <select name="categoria" class="form-select" required>
                    <option value="" disabled selected>Seleccione categoria</option>
                    {{#categorias}}
                        <option value="{{id}}">{{nombre}}</option>
                    {{/categorias}}
                </select>
            </div>
            <div class="col-md-6">
                <label class="form-label fw-bold">Opciones incorrectas</label>
                <input type="text" name="incorrecta1" class="form-control mb-2" placeholder="Escriba la opcion">
                <input type="text" name="incorrecta2" class="form-control mb-2" placeholder="Escriba la opcion">
                <input type="text" name="incorrecta3" class="form-control" placeholder="Escriba la opcion">
            </div>

            <div class="col-md-6">
                <label class="form-label fw-bold">Pregunta</label>
                <textarea name="pregunta" rows="4" class="form-control" placeholder="Escriba su pregunta"></textarea>
            </div>

            <div class="col-md-6 d-flex flex-column justify-content-end"></div>

            <div class="col-md-6">
                <label class="form-label fw-bold">Opcion correcta</label>
                <input type="text" name="correcta" class="form-control" placeholder="Escriba la opcion">
            </div>
            <div class="col-md-6 d-flex align-items-end justify-content-end">
                <button type="submit" class="btn btn-orange btn-rounded px-4">Sugerir</button>
            </div>
        </div>
    </form>
</div>

</body>
</html>
