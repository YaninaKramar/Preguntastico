<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Perfil de Usuario</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

</head>
<style>
    body{
        background: linear-gradient(to bottom, #a8e6ce,#ffffff);
        min-height: 100vh;
        margin: 0;
        font-family: Arial, sans-serif;
    }
    .perfil-header {
        background-color: #ff6600;
        padding: 20px;
        color: white;
        display: flex;
        justify-content: space-between;
        align-items: center;
    }
    .perfil-info{
        display: flex;
        flex-direction: column;
    }
    .foto-perfil {
        width: 200px;
        height: 200px;
        object-fit: cover;
        border-radius: 50%;
    }
    .contenedor {
        display: flex;
        justify-content: space-evenly;
        gap: 20px;
        margin-top: 20px;
    }
    .columna-izquierda {
        width: 30%;
        display: flex;
        flex-direction: column;
        gap: 20px;
        align-items: center;
    }
    .columna-derecha {
        width: 50%;
    }
    .boton-volver {
        left: 20px;
        top: 20px;
        background-color: red;
        color: white;
        border-radius: 20px;
        padding: 10px 20px;
        font-weight: bold;
        text-decoration: none;
        display: inline-block;
    }
    .box {
        background-color: #ff6600;
        padding: 15px;
        color: white;
        border-radius: 8px;
    }
    .mapa {
        width: 100%;
        height: 300px;
        border: none;
    }
    .btn-mapa {
        display: block;
        margin-bottom: 10px;
        padding: 10px;
        background-color: #003b5c;
        color: white;
        text-align: center;
        border: none;
        cursor: pointer;
        text-decoration: none;
    }
</style>
<body>
<div class="perfil-header">
    <div class="perfil-info">
        <p><a href="/Preguntastico/TP-FinalPW2/lobby/show" class="boton-volver">Volver al lobby</a></p>
        <h1>{{usuario.nombre_completo}}</h1>
        <h3>Puntaje total: {{puntaje_total}}</h3>
    </div>
    <div class="perfil-foto">
    <img class="foto-perfil" src="{{usuario.foto_perfil}}" alt="Foto de perfil" />
    <p>{{usuario.fecha_nac}}</p>
    </div>
</div>

<div class="contenedor">
    <div class="columna-izquierda">
        <div class="box">
            <h2>Partidas jugadas</h2>
            {{#partidas}}
                <p>📅 {{fecha}} - 🏆 {{puntaje_final}} puntos</p>
            {{/partidas}}
        </div>

        <div class="box">
            <h2>Código QR del perfil</h2>
            <img src="{{qr_url}}" alt="QR del perfil" />
        </div>
    </div>

    <div class="columna-derecha">
        <a class="btn-mapa" href="{{maps_url}}" target="_blank">Ubicación</a>
        <iframe class="mapa"
                src="https://www.google.com/maps?q={{usuario.ciudad}}&output=embed">
        </iframe>
    </div>
</div>
</body>
</html>