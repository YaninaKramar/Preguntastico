<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Listado de Preguntas</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body{
            background: linear-gradient(to bottom, #a8e6ce,#ffffff);
            min-height: 100vh;
            margin: 0;
            font-family: Arial, sans-serif;
        }
        .btn-rounded{
            border-radius: 20px;
            padding: 8px 20px;
        }
        .btn-darkblue{
            background-color: #00334e;
            color: white;
            text-align: center;
            text-decoration: none;
            border: none;
        }
        .btn-darkblue:hover{
            background-color: #005b8e;
            color:white;
        }
        h1{
            color:#000;
        }
        .sidebar .btn{
            width: 170px;
        }
        .btn-orange{
            background-color:#ff6f20;
            color:white;
        }
        .btn-orange:hover{
            background-color:#ff8c42;
            color:white;
        }
        .list-container{
            max-height:70vh;
            overflow-y:auto;
            padding-right:8px;
        }
        .list-container::-webkit-scrollbar{
            width:8px;
        }
        .list-container::-webkit-scrollbar-thumb{
            background:rgba(0,0,0,0.2);
            border-radius:4px;
        }
        .question-row{
            display:flex;
            align-items:center;
            gap:15px;
        }
        .question-box{
            background-color:#ffffff;
            border:1px solid #00000040;
            border-radius:20px;
            padding:20px;
            min-width:200px;
            text-align:center;
        }
        .options{
            display:flex;
            flex-wrap:wrap;
            gap:10px;
            flex-grow:1;
        }
        .option{
            background-color:#ffffff;
            padding:8px 12px;
            border:1px solid #888;
            border-radius:20px;
            font-size:14px;
            white-space:nowrap;
        }
        .actions{
            display:flex;
            flex-direction:column;
            gap:8px;
        }
        .btn-edit{
            background-color:#ffbe0b;
            color:white;
            border:none;
        }
        .btn-edit:hover{
            background-color:#f9d157;
            color:white;
        }
        .btn-delete{
            background-color:#e63946;
            color:white;
            border:none;
        }
        .btn-delete:hover{
            background-color:#ff4455;
            color:white;
        }
    </style>
</head>
<body class="p-4">

<div class="d-flex justify-content-between align-items-center mb-4">
    <a href="" class="btn-darkblue btn-rounded">Mi Perfil</a>
    <h1 class="text-center flex-grow-1 fw-bold">Listado de preguntas</h1>
    <a class="btn-darkblue btn-rounded" href="">Cerrar Sesión</a>
</div>

<div class="row">
    <!-- ---------- barra lateral ---------- -->
    <div class="col-md-2 sidebar d-flex flex-column gap-3 mb-4">
        <a href="" class="btn btn-orange btn-rounded">Ver Preguntas<br>reportadas</a>
        <a href="" class="btn-darkblue btn-rounded">Volver al lobby</a>
    </div>

    <!-- ---------- listado de preguntas ---------- -->
    <div class="col-md-10 list-container">
        <div class="question-row mb-4">
            <!-- Texto de la pregunta -->
            <div class="question-box flex-shrink-0">{{pregunta}}</div>

            <!-- Opciones -->
            <div class="options">
                <span class="option">{{opcion}}</span>
                <span class="option">{{opcion}}</span>
                <span class="option">{{opcion}}</span>
                <span class="option">{{opcion}}</span>
            </div>

            <!-- Acciones -->
            <div class="actions">
                <a href="" class="btn btn-edit btn-rounded">Editar</a>
                <a href="" class="btn btn-delete btn-rounded">Eliminar</a>
            </div>
        </div>
        <div class="question-row mb-4">
            <!-- Texto de la pregunta -->
            <div class="question-box flex-shrink-0">{{pregunta}}</div>

            <!-- Opciones -->
            <div class="options">
                <span class="option">{{opcion}}</span>
                <span class="option">{{opcion}}</span>
                <span class="option">{{opcion}}</span>
                <span class="option">{{opcion}}</span>
            </div>

            <!-- Acciones -->
            <div class="actions">
                <a href="/Preguntastico/TP-FinalPW2/pregunta/editar/{{id}}" class="btn btn-edit btn-rounded">Editar</a>
                <a href="/Preguntastico/TP-FinalPW2/pregunta/eliminar/{{id}}" class="btn btn-delete btn-rounded">Eliminar</a>
            </div>
        </div>
    </div>
</div>

</body>
</html>
