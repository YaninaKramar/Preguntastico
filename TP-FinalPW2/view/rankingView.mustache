<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Ranking</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(to bottom, #a8e6ce, #ffffff);
            min-height: 100vh;
            font-family: Arial, sans-serif;
            padding: 20px;
        }

        .btn-rounded {
            border-radius: 20px;
            padding: 10px 20px;
            font-weight: bold;
        }

        .btn-darkblue {
            background-color: #00334e;
            color: white;
            border: none;
        }

        h1 {
            font-weight: bold;
            color: #ff6600;
            text-align: center;
            margin-bottom: 30px;
        }

        .ranking-box {
            background-color: white;
            border: 1px solid black;
            border-radius: 20px;
            padding: 20px;
            max-width: 400px;
            margin: auto;
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
            overflow-y: auto;
            max-height: 320px;
        }

        .ranking-item {
            display: flex;
            align-items: center;
            margin: 10px 0;
            border-radius: 20px;
            padding: 8px 12px;
        }

        .ranking-top {
            background-color: #ff914d;
            color: white;
        }

        .ranking-other {
            background-color: #d9d9d9;
            color: #00334e;
        }

        .ranking-position {
            font-weight: bold;
            font-size: 22px;
            margin-right: 15px;
            width: 30px;
            text-align: center;
        }

        .volver-btn {
            background-color: #003b5c;
            color: white;
            font-weight: bold;
        }

        .medal {
            width: 25px;
            margin-right: 10px;
        }

        .top-name {
            font-weight: bold;
        }

        a {
            text-decoration: none;
            color: inherit;
        }

        .top-bar {
            display: flex;
            justify-content: space-between;
            margin-bottom: 30px;
        }
    </style>
</head>
<body>

<div class="top-bar">
    <button class="btn-darkblue btn-rounded">Mi Perfil</button>
    <button class="btn-darkblue btn-rounded">Cerrar Sesión</button>
</div>

<h1>RANKING</h1>

<div class="ranking-box">
    <div class="ranking-item ranking-top">
        <span class="ranking-position">1</span>
        <span class="top-name">{{usuario}} - {{puntos}} pts</span>
    </div>
    <div class="ranking-item ranking-top">
        <span class="ranking-position">2</span>
        <span class="top-name">{{usuario}} - {{puntos}} pts</span>
    </div>
    <div class="ranking-item ranking-top">
        <span class="ranking-position">3</span>
        <span class="top-name">{{usuario}} - {{puntos}} pts</span>
    </div>
    <div class="ranking-item ranking-other">
        <span class="ranking-position">4</span>
        <span>{{usuario}} - {{puntos}} pts</span>
    </div>
    <div class="ranking-item ranking-other">
        <span class="ranking-position">5</span>
        <span>{{usuario}} - {{puntos}} pts</span>
    </div>
    <div class="ranking-item ranking-other">
        <span class="ranking-position">6</span>
        <span>{{usuario}} - {{puntos}} pts</span>
    </div>
</div>

<div class="text-center mt-4">
    <a href="/lobby/show" class="btn volver-btn btn-rounded">Volver</a>
</div>

</body>
</html>
