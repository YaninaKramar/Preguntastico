<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Preguntas Sugeridas</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(to bottom, #a8e6ce, #ffffff);
            min-height: 100vh;
            margin: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        .btn-rounded {
            border-radius: 20px;
            padding: 8px 20px;
        }

        h1 {
            color: #00334e;
        }

        .question-row {
            background-color: #ffffff;
            border-radius: 15px;
            padding: 20px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.08);
            transition: transform 0.2s ease, box-shadow 0.2s ease;
        }

        .question-row:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 15px rgba(0, 0, 0, 0.12);
        }

        .question-box {
            font-weight: bold;
            font-size: 18px;
            color: #333;
            margin-bottom: 12px;
        }

        .options {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
            margin-bottom: 15px;
        }

        .option {
            background-color: #f1f1f1;
            color: #333;
            padding: 8px 14px;
            border-radius: 20px;
            font-size: 14px;
            border: 1px solid #ccc;
        }

        .actions {
            text-align: right;
        }

        .boton-volver {
            left: 20px;
            top: 20px;
            background-color: red;
            color: white;
            border-radius: 20px;
            padding: 10px 20px;
            font-weight: bold;
            text-decoration: none;
            display: inline-block;
        }

        .btn-sm {
            padding: 6px 16px;
        }

        .btn-success {
            background-color: #28a745;
            border: none;
        }

        .btn-success:hover {
            background-color: #218838;
        }

        .btn-danger {
            background-color: #dc3545;
            border: none;
        }

        .btn-danger:hover {
            background-color: #c82333;
        }
    </style>
</head>
<body class="p-4">
<p><a href="/editor/listado" class="boton-volver">&larr;Volver al panel</a></p>
<div class="container">
    <h1 class="text-center fw-bold mb-5">Preguntas reportadas</h1>

    {{#preguntasSugeridas}}
        <div class="question-row mb-4">
            <div class="question-box">{{texto}}</div>

            <div class="options">
                {{#opciones}}
                    {{#correcta}}
                        <span class="option bg-success text-white">{{texto}}</span>
                    {{/correcta}}
                    {{^correcta}}
                        <span class="option bg-danger text-white">{{texto}}</span>
                    {{/correcta}}
                {{/opciones}}
            </div>


            <div class="actions">
                <a href="/editor/aprobarSugerida/id={{id}}" class="btn btn-success btn-sm me-2">Aprobar</a>
                <a href="/editor/rechazarSugerida/id={{id}}" class="btn btn-danger btn-sm">Rechazar</a>
            </div>
        </div>
    {{/preguntasSugeridas}}

    {{^preguntasSugeridas}}
        <p class="text-center fs-5 mt-5">No hay preguntas reportadas por ahora.</p>
    {{/preguntasSugeridas}}

</div>

</body>
</html>